// import Database from "./Database";

// export default abstract class Table<DB extends Database = Database, ROW> {
// 	public constructor (protected readonly name: string, protected readonly database: DB) {

// 	}

// 	public addRow(initializer: (row: Row))
// }
